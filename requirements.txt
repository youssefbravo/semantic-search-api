# --- API layer ---
fastapi==0.115.6
uvicorn[standard]==0.34.0
python-multipart==0.0.20        # multipart/form-data for file uploads
pydantic==2.10.4
pydantic-settings==2.7.1

# --- Database ---
SQLAlchemy==2.0.36
psycopg[binary]==3.2.3          # psycopg3 driver
pgvector==0.3.6                 # SQLAlchemy Vector type + ops

# --- Background processing ---
celery[redis]==5.4.0
redis==5.2.1
prometheus-fastapi-instrumentator==7.0.2

# --- ML / embeddings ---
sentence-transformers==3.3.1
transformers==4.47.1
torch==2.5.1                    # CPU build is fine for bge-small
numpy==2.2.1

# --- Parsing ---
pypdf==5.1.0

# --- Eval / benchmark (phase 4) --- (numpy pinned above; metrics are hand-rolled)
matplotlib==3.10.0              # benchmark + chunk-sweep charts

# --- Tests (phase 6) ---
pytest==8.3.4
httpx==0.28.1                   # FastAPI TestClient transport
